import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'app.paypeer.sol',
  appName: 'paypeer',
  webDir: 'out',
  bundledWebRuntime: false
};

export default config;
